# Demo fixtures

This folder is reserved for screen recordings and screenshots generated from a local test install. Do not commit real provider responses, credentials, cookies, account IDs, user paths, or exported diagnostics.

For a deterministic visual smoke test, use the redacted fixture in `TinyUsageDomain/Tests` and replace screenshots with synthetic data only. The launch page intentionally ships with a vector architecture illustration instead of provider logos or personal screenshots.
